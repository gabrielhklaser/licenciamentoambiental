# Requisitos do Sistema de Verificação de Licenciamento Ambiental (4 fases)
# Instalação:  python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt

# --- Fase 1: Parser HTML (DOM + RegEx) ---
beautifulsoup4>=4.12
lxml>=4.9

# --- Fase 2: Persistência e agentes determinísticos ---
SQLAlchemy>=2.0
geoalchemy2>=0.14        # PostGIS (SIRGAS 2000); opcional fora do PostgreSQL

# --- Fase 3: Auditor técnica ---
pydantic>=2.5
pypdf>=4.0               # extração de texto de laudos PDF
# Para o provedor LLM de produção (opcional; usa structured output):
# langchain>=0.2
# langchain-openai>=0.1

# --- Fase 4: Dashboard e ofícios ---
streamlit>=1.31
pandas>=2.0
python-docx>=1.1
openpyxl>=3.1             # leitura de planilhas Excel (.xlsx) no upload

# OCR de documentos enviados como imagem (png/jpg) - OPCIONAL:
# requer o binário tesseract-ocr instalado no servidor (idealmente com pt-BR):
# sudo apt install tesseract-ocr tesseract-ocr-por
# pytesseract>=0.3

# Dependência de DEV (apenas para regenerar os laudos de exemplo):
PyMuPDF>=1.24          # renderiza páginas de PDF p/ OCR
rapidocr-onnxruntime>=1.3  # OCR em CPU (PDFs escaneados)
opencv-python-headless>=4.9  # backend do RapidOCR sem libGL
reportlab>=4.0        # skill pdf: exportar o parecer técnico em PDF
# NB: se o OpenCV reclamar de libGL.so.1: pip uninstall -y opencv-python && pip install opencv-python-headless
